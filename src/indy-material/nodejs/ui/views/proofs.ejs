<div id="proofs" class="tab" style="display:none;">
    <form method="POST" action="/api/proofs/send_request">
        <div class="w3-half">
            <div class="w3-container w3-card w3-padding w3-margin">
                <label>Relationship: </label>
                <select class="w3-select" name="their_relationship_did" required>
                    <%for(let relationship of relationships) {%>
                    <option value="<%=relationship.their_did%>"><%=relationship.metadata.name%></option>
                    <%}%>
                </select>
                <br><br>
                <label>Select a Proof Request:</label>
                <select id="currentProofRequest" onchange="showDiv(this.value, 'proofRequestsClass')" class="w3-select" name="proof_request_id" required>
                    <%for(let pr of Object.keys(proofRequests)) {%>
                    <option value="<%=pr%>"><%=pr%></option>
                    <%}%>
                    <option value="proofRequestOther">Other (Paste Proof Request Here) --></option>
                </select>
                <br><br>
                <input class="w3-btn w3-teal w3-border" type="submit" value="submit">
            </div>
        </div>
        <div class="w3-half">
            <%for(let pr of Object.keys(proofRequests)) {%>
                <h3 class="w3-bar w3-border" style="width:100%;text-align: center;color: white;margin-top: 15px;margin-bottom:0px;padding:10px">proofRequest</h3>
                <div id="<%=proofRequests[pr].name%>" class="proofRequestsClass" style="width:100%;display:light-gray; margin-right: 30px">
                    <textarea style="width:100%;padding:30px" rows="20" readonly><%=proofRequests[pr].string%></textarea>
                </div>
            <%}%>
            <div id="proofRequestOther" class="proofRequestsClass" style="width:100%;display:none;">
                <textarea name="manual_entry" style="width:100%;" rows="20"></textarea>
            </div>
        </div>
    </form>
</div>